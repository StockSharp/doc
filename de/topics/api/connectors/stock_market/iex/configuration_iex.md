> [!CAUTION]
> **Die von diesem Konnektor verwendete IEX-Handels-API ist nicht mehr verfügbar. Der Konnektor funktioniert nicht; die Dokumentation dient nur noch als Referenz.**

# Konfiguration IEX

Um mit dem Connector zu arbeiten, müssen Sie das **Token** angeben. Das **Token** wird vom Broker bereitgestellt. Für den API-Zugriff wird empfohlen, den Broker zu kontaktieren.

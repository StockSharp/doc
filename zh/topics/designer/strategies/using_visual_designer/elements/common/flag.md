# 标志

![Designer 标志](../../../../../../images/designer_flag_00.png)

“Flag”组件用于管理二进制标志。该标志可以根据传入信号进行设置或重置。

## 输入端口

- **触发器**：接收除 `False` 以外的任意值。收到第一个符合条件的值时设置标志。如果标志已经设置，则在重置之前会忽略后续信号。
- **重置**：接收除 `False` 以外的任意值。重置标志，使其可以响应后续触发信号。

## 输出端口

- **信号**：设置标志时输出信号。

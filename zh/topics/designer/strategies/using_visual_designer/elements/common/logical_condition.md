# 逻辑条件

![Designer The logical condition 00](../../../../../../images/designer_logical_condition_00.png)

该元素用于计算包含两个参数的逻辑公式。

## 输入端口

- **Flag** – 标志值（表示状态，包含两个值：上（true）和下（false））。
- **Flag** – 标志值（表示状态，包含两个值：上（true）和下（false））。

## 输出端口

- **Flag** – 标志值（表示状态，包含两个值：上（true）和下（false））。

## 参数

- **Operator** - 预定义的逻辑公式集合：AND、OR 和 Exclusive OR（异或）。

## 另请参阅

[Formula](formula.md)

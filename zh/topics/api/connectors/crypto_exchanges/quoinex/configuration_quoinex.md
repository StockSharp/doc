> [!CAUTION]
> **QUOINEX 交易所更名为 Liquid 后，Liquid 也已停止运营。该连接器已无法使用；文档仅保留供参考。**

# Quoinex 配置

要使用该连接器，需要在交易所个人账户中生成 **访问密钥** 和 **密钥**，并在连接设置中指定它们。

> [!TIP]
> 注意，**访问密钥** 和 **密钥** 不是 **登录名** 和 **密码**。**访问密钥** 和 **密钥** 需要在个人账户中单独生成。使用连接器时不需要 **登录名** 和 **密码**，也不需要在任何地方填写它们。

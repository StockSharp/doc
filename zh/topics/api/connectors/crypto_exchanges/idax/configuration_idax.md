> [!WARNING]
> 该交易所已永久关闭（2019年11月 — 关闭）。此连接器不再可用。文档已保留以供历史参考。

# Idax 配置

要使用该连接器，需要在交易所个人账户中生成 **Key** 和 **Secret**，并在连接设置中指定它们。

> [!TIP]
> 注意，**Key** 和 **Secret** 不是 **Login** 和 **Password**。**Key** 和 **Secret** 需要在个人账户中单独生成。使用连接器时不需要 **Login** 和 **Password**，也不需要在任何地方填写它们。

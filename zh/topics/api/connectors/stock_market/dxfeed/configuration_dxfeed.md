# 连接器配置: dxFeed

从服务提供商获取身份验证凭据，并指定连接参数。

- `Address` - 服务地址。 默认值: `wss://demo.dxfeed.com/dxlink-ws`.
- `Token` - 身份验证凭据。
- `AggregationPeriod` - 时间间隔。
- `MarketDepthLevels` - 连接器的数值参数。 默认值: `20`.
- `MarketDepthSources` - 连接参数。 默认值: `NTV`.

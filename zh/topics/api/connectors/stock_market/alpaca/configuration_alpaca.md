# Alpaca 配置

要使用连接器，需要在经纪商的个人账户中生成 **访问密钥 (API 密钥)** 和 **密钥**，并在连接设置中指定它们。

> [!TIP]
> 需要注意的是，**访问密钥** 和 **密钥** 并不是 **登录名** 和 **密码**。**访问密钥** 和 **密钥** 是在您的个人账户中单独生成的。使用连接器时，您不需要 **登录名** 和 **密码**，也不需要在任何地方指定它们。

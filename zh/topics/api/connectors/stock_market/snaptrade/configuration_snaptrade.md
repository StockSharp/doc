# 连接器配置: SnapTrade

从服务提供商获取身份验证凭据，并指定连接参数。

- `ClientId` - 账户或客户端标识符。
- `ConsumerKey` - 身份验证凭据。
- `UserId` - 账户或客户端标识符。
- `UserSecret` - 身份验证凭据。
- `AccountId` - 账户或客户端标识符。
- `PollingInterval` - 时间间隔。 默认值: `TimeSpan.FromMinutes(1)`.

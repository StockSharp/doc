# Kotak Neo 连接器配置

获取 Kotak Neo 交易 API v2 所需的凭据、服务器地址和账户标识符，然后指定以下连接器设置。

- **消费者密钥** - 属性 `ConsumerKey`。
- **手机号码** - 属性 `MobileNumber`。
- **用户代码** - 属性 `UserCode`。
- **MPIN** - 属性 `Mpin`。
- **TOTP 密钥** - 属性 `TotpSecret`。
- **默认产品** - 属性 `DefaultProduct`。默认值为 `Intraday`。

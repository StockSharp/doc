# Tiger Brokers 连接器配置

获取 Tiger Brokers OpenAPI 所需的凭据、服务器地址和账户标识符，然后指定以下连接器设置。

- **Tiger 标识符** - 属性 `TigerId`。
- **账户** - 属性 `Account`。
- **许可证** - 属性 `License`。默认值为 `Singapore`。
- **私钥** - 属性 `PrivateKey`。
- **令牌** - 属性 `Token`。
- **自动获取行情权限** - 属性 `AutoGrabPermission`。默认值为 `true`。

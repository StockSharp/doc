# TradeZero 连接器配置

在 TradeZero 门户中创建 API 凭据，并在连接器设置中指定这些凭据。

- **访问密钥** - `TZ-API-KEY-ID` 的值。
- **密钥** - `TZ-API-SECRET-KEY` 的值。
- **默认路由** - 账户路由端点返回的可选首选订单路由。

如果默认路由为空，连接器会自动选择兼容的有效路由。模拟账户和实盘账户使用同一 API 主机；账户环境由凭据决定。

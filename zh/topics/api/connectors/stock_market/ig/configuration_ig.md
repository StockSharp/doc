# IG Markets 连接器配置

获取 IG REST 和流式 API 所需的凭据、服务器地址和账户标识符，然后指定以下连接器设置。

- **API 密钥** - 属性 `ApiKey`。
- **用户名** - 属性 `UserName`。
- **密码** - 属性 `Password`。
- **账户标识符** - 属性 `AccountId`。
- **环境** - 属性 `Environment`。默认值为 `Demo`。
- **加密密码** - 属性 `EncryptPassword`。

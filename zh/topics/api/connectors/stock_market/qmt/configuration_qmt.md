# 连接器配置: QMT

准备服务提供商的本地网关或客户端，并指定连接参数。

- `GatewayHost` - 服务地址。 默认值: `127.0.0.1`.
- `GatewayPort` - 连接器的数值参数。 默认值: `58630`.
- `GatewayToken` - 身份验证凭据。
- `ReconnectAttempts` - 连接器的数值参数。 默认值: `10`.
- `RequestTimeout` - 时间间隔。 默认值: `30`.

# Robinhood 连接器配置

获取 Robinhood 智能代理交易 所需的凭据、服务器地址和账户标识符，然后指定以下连接器设置。

- **令牌** - 属性 `Token`。
- **地址** - 属性 `Address`。默认值为 `https://agent.robinhood.com/mcp/trading`。
- **轮询间隔** - 属性 `PollingInterval`。默认值为 `2 s`。

# FYERS connector configuration

Obtain the credentials, endpoints, and account identifiers required by FYERS API v3, then specify the following connector settings.

- **Client ID** - property `ClientId`.
- **Token** - property `Token`.
- **Default product** - property `DefaultProduct`. Default value: `Intraday`.

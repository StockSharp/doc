# Connector configuration: Zerodha Kite Connect

Obtain the credentials from the provider and specify the connection parameters.

- `ApiKey` - authentication credential.
- `ApiSecret` - authentication credential.
- `Token` - authentication credential.
- `RequestToken` - authentication credential.
- `DefaultProduct` - connector mode or option. Default value: `ZerodhaProducts.Intraday`.

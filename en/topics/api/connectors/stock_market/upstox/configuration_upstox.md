# Upstox connector configuration

Obtain the credentials, endpoints, and account identifiers required by Upstox API v3, then specify the following connector settings.

- **Token** - property `Token`.
- **Demo mode** - property `IsDemo`.
- **Default product** - property `DefaultProduct`. Default value: `Delivery`.

# Public.com connector configuration

Obtain the credentials, endpoints, and account identifiers required by Public Investing API, then specify the following connector settings.

- **Token** - property `Token`.
- **Polling interval** - property `PollingInterval`. Default value: `2 s`.

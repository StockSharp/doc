# tastytrade connector configuration

Obtain the credentials, endpoints, and account identifiers required by tastytrade Open API, then specify the following connector settings.

- **Token** - property `Token`.
- **Client secret** - property `ClientSecret`.
- **Access scopes** - property `Scopes`. Default value: `Read | Trade`.
- **Demo mode** - property `IsDemo`.

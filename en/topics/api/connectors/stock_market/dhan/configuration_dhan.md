# DhanHQ connector configuration

Obtain the credentials, endpoints, and account identifiers required by DhanHQ API v2, then specify the following connector settings.

- **Client ID** - property `ClientId`.
- **Token** - property `Token`.
- **Default product** - property `DefaultProduct`. Default value: `Intraday`.

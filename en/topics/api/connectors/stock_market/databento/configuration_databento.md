# Connector configuration: Databento

Obtain the credentials from the provider and specify the connection parameters.

- `Key` - authentication credential.
- `Dataset` - connection parameter. Default value: `GLBX.MDP3`.
- `LiveAddress` - service address.
- `HistoricalAddress` - service address. Default value: `https://hist.databento.com/v0/timeseries.get_range`.
- `Symbology` - connector mode or option. Default value: `DatabentoSymbologyTypes.RawSymbol`.

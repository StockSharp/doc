# Connector configuration: AscendEX

Configure the following properties before connecting to AscendEX. The list is verified against [AscendExMessageAdapter](xref:StockSharp.AscendEx.AscendExMessageAdapter).

## Basic settings

The connection editor shows these settings first.

- `Key` (`SecureString`)
- `Secret` (`SecureString`)
- `AccountGroup` (`int`)
- `SpotAccountType` (`AscendExSpotAccountTypes`)
- `RestEndpoint` (`string`)
- `SpotWebSocketEndpoint` (`string`)
- `FuturesWebSocketEndpoint` (`string`)

## See also

[Graphical configuration](graphical_configuration_ascendex.md)

[Adapter initialization](adapter_initialization_ascendex.md)

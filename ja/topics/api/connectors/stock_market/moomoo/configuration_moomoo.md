# Moomoo コネクタの設定

Moomoo OpenAPI・OpenD に必要な認証情報、サーバーアドレス、口座識別子を取得し、次のコネクタ設定を指定します。

- **アドレス** - プロパティ `Address`。既定値は `127.0.0.1:11111` です。
- **パスワード** - プロパティ `Password`。
- **デモモード** - プロパティ `IsDemo`。既定値は `true` です。

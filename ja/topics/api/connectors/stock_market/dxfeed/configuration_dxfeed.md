# コネクターの設定: dxFeed

プロバイダーから認証情報を取得し、接続パラメーターを指定します。

- `Address` - サービスのアドレスです。 既定値: `wss://demo.dxfeed.com/dxlink-ws`.
- `Token` - 認証用の資格情報です。
- `AggregationPeriod` - 時間間隔です。
- `MarketDepthLevels` - コネクターの数値パラメーターです。 既定値: `20`.
- `MarketDepthSources` - 接続パラメーターです。 既定値: `NTV`.

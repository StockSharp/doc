# TradeStation コネクタの設定

TradeStation API v3 に必要な認証情報、サーバーアドレス、口座識別子を取得し、次のコネクタ設定を指定します。

- **トークン** - プロパティ `Token`。
- **デモモード** - プロパティ `IsDemo`。
- **既定ルート** - プロパティ `DefaultRoute`。既定値は `Intelligent` です。

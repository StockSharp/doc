# tastytrade コネクタの設定

tastytrade Open API に必要な認証情報、サーバーアドレス、口座識別子を取得し、次のコネクタ設定を指定します。

- **トークン** - プロパティ `Token`。
- **クライアントシークレット** - プロパティ `ClientSecret`。
- **アクセス範囲** - プロパティ `Scopes`。
- **デモモード** - プロパティ `IsDemo`。

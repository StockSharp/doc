# Lime Trader コネクタの設定

Lime API に必要な認証情報、サーバーアドレス、口座識別子を取得し、次のコネクタ設定を指定します。

- **ユーザー名** - プロパティ `Login`。
- **パスワード** - プロパティ `Password`。
- **クライアント ID** - プロパティ `ClientId`。
- **クライアントシークレット** - プロパティ `ClientSecret`。

# TradeZero コネクタの設定

TradeZero ポータルで API 認証情報を作成し、コネクタの設定に指定します。

- **キー** - `TZ-API-KEY-ID` の値。
- **シークレット** - `TZ-API-SECRET-KEY` の値。
- **既定ルート** - 口座ルートのエンドポイントから返される、任意の優先注文ルート。

既定ルートが空の場合、コネクタは互換性のある有効なルートを自動的に選択します。シミュレーション口座と実口座は同じ API ホストを使用し、認証情報によって口座環境が決まります。

# Robinhood コネクタの設定

Robinhood エージェント取引 に必要な認証情報、サーバーアドレス、口座識別子を取得し、次のコネクタ設定を指定します。

- **トークン** - プロパティ `Token`。
- **アドレス** - プロパティ `Address`。既定値は `https://agent.robinhood.com/mcp/trading` です。
- **ポーリング間隔** - プロパティ `PollingInterval`。既定値は `2 s` です。

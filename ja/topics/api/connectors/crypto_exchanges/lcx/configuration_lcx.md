# コネクタ設定: LCX

LCX に接続する前に、次のアダプタープロパティを設定します。この一覧は [LcxMessageAdapter](xref:StockSharp.LCX.LcxMessageAdapter) の実装で確認されています。

## 基本設定

接続エディターでは、これらの設定が最初に表示されます。

- `Key` (`SecureString`)
- `Secret` (`SecureString`)
- `RestEndpoint` (`string`)
- `KlineEndpoint` (`string`)
- `WebSocketEndpoint` (`string`)
- `ApiVersion` (`string`)
- `PrivatePollingInterval` (`TimeSpan`)

## 関連項目

[グラフィカル設定](graphical_configuration_lcx.md)

[アダプターの初期化](adapter_initialization_lcx.md)

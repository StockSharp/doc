# Configuración del conector: AscendEX

Configure las siguientes propiedades antes de conectarse a AscendEX. La lista se ha verificado con [AscendExMessageAdapter](xref:StockSharp.AscendEx.AscendExMessageAdapter).

## Configuración básica

El editor de conexiones muestra primero estos parámetros.

- `Key` (`SecureString`)
- `Secret` (`SecureString`)
- `AccountGroup` (`int`)
- `SpotAccountType` (`AscendExSpotAccountTypes`)
- `RestEndpoint` (`string`)
- `SpotWebSocketEndpoint` (`string`)
- `FuturesWebSocketEndpoint` (`string`)

## Véase también

[Configuración gráfica](graphical_configuration_ascendex.md)

[Inicialización del adaptador](adapter_initialization_ascendex.md)

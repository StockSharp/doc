> [!WARNING]
> Esta bolsa foi encerrada permanentemente (maio de 2023 - encerrada). Este conector já não está operacional. A documentação é preservada para referência histórica.

# Configuração Hotbit

Para trabalhar com um conector, é necessário gerar a **chave (API Key)** e o **segredo** na conta pessoal da bolsa e especificá-los nas definições de ligação.

> [!TIP]
> Tenha em atenção que a **chave** e o **segredo** não são o **nome de utilizador** e a **palavra-passe**. A **chave** e o **segredo** são gerados separadamente na sua conta pessoal. Não precisa do **nome de utilizador** e da **palavra-passe** para trabalhar com o conector e não precisa de os especificar em lado nenhum.

